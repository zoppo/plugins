if (( $+commands[unrar] )); then
  unrar e -ad "$1"
elif (( $+commands[rar] )); then
  rar x -ad "$1"
else
  print "Please install unrar or rar first." >&2
  return 1
fi

# vim: ft=zsh sts=2 ts=2 sw=2 et fdm=marker fmr={{{,}}}
